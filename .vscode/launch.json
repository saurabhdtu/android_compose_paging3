{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gradle",
            "request": "launch",
            "name": "Gradle assembleDebug",
            "tasks": [
                "assembleDebug"
            ],
            "projectRoot": "${workspaceFolder}"
        }
    ]
}